[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow = { workspace = true, default-features = true }
clap = { workspace = true, features = ["derive", "help", "usage", "error-context"] }
which = { workspace = true, default-features = true }
